#!/bin/ksh
#

daemon="/usr/local/bin/nomad agent"
daemon_flags="-config /etc/nomad/nomad.hcl"

. /etc/rc.d/rc.subr

rc_bg=YES

rc_stop() {
    pkill -INT -T "${daemon_rtable}" -xf "${pexp}"
}

rc_cmd $1
